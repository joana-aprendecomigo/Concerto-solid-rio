// TODO: This is a placeholder. The real application component (~3000 lines) still needs to be
// added here from the original source file, including the two base64 logo data URIs below.
// See README-SETUP.md for what remains.

export const LOGO_YME_DARK = "";
export const LOGO_YME_LIGHT = "";

export default function App() {
  return (
    <div
      style={{
        fontFamily: "system-ui, sans-serif",
        padding: 40,
        color: "#131A2C",
      }}
    >
      <h1 style={{ fontSize: 20, marginBottom: 8 }}>Concerto Solidário</h1>
      <p style={{ color: "#5B6478", fontSize: 14, lineHeight: 1.6 }}>
        Project scaffold is set up. The main application component still needs to
        be added to <code>src/App.jsx</code>.
      </p>
    </div>
  );
}
